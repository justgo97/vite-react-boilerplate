const Navbar = () => {
  return (
    <nav className="navbar bg-body-tertiary">
      <div className="container-fluid justify-content-center">
        <span className="navbar-text fs-4 fw-bold text-success">
          Vite + React boilerplate
        </span>
      </div>
    </nav>
  );
};

export default Navbar;
